refs:
  #run with ./talos_graphics --loader ../etc/talos/joint_replay/trajectory.yaml -f  -w --damage true --sim_freq 500 --control_freq 500  -a servo --cut leg_left_4_link
  # posture: 
  #   q: q_one_knee.csv
  #   size: 36
  #run with ./talos_graphics --loader ../etc/talos/joint_replay/trajectory.yaml -f -w --damage true --sim_freq 500 --control_freq 500  -a servo --cut leg_left_1_link
  # posture:
  #   q: q_one_leg.csv
  #   size: 33
  #run with ./talos_graphics --loader ../etc/talos/joint_replay/trajectory.yaml -f -w true --sim_freq 500 --control_freq 500  -a servo
  posture:
    q: taichi.csv
    size: 39